# 3D 物理沙漏模拟器

一个精致的网页端 3D 沙漏演示，支持：

- 手动翻转沙漏
- 实时调节沙粒数量
- 鼠标 / 触屏旋转视角
- iOS / Android / 桌面浏览器兼容处理（`viewport-fit=cover`、安全区、`touchmove` 回退、横竖屏变化监听）

## 运行

```bash
python3 -m http.server 8080
```

然后访问 <http://localhost:8080>。
